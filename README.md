# Inventory Management System

Веб-приложение для управления инвентаризацией (офисное оборудование, книги в библиотеке, документы в HR-отделе и т.д.). Пользователи создают произвольные "инвентари" (шаблоны с полями), а другие пользователи заполняют их "элементами" (конкретными записями).

## 🚀 Технический стек

- **PHP** 8.4
- **Symfony** 8.0
- **PostgreSQL** (или MySQL)
- **Bootstrap** 5.3 (CSS Framework)
- **Doctrine** ORM
- **Asset Mapper** для фронтенда
- **Stimulus** для JavaScript
- **OAuth2** (Google, Facebook)
- Полнотекстовый поиск

## 📋 Системные требования

- PHP >= 8.4
- PostgreSQL >= 16 (или MySQL >= 8.0)
- Node.js >= 18 (для сборки assets)
- Composer >= 2.0
- Docker & Docker Compose (опционально, для локальной разработки)

## ⚡ Быстрый старт

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd cor_proj
```

### 2. Установка зависимостей PHP

```bash
composer install
```

### 3. Установка зависимостей JavaScript

```bash
npm install
```

### 4. Настройка переменных окружения

Создайте файл `.env.local` на основе `.env`:

```bash
cp .env .env.local
```

Настройте следующие переменные в `.env.local`:

```env
# База данных
DATABASE_URL="postgresql://app:!ChangeMe!@127.0.0.1:5432/app?serverVersion=16&charset=utf8"

# OAuth2 (получите ключи от Google и Facebook)
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
FACEBOOK_CLIENT_ID=your_facebook_client_id
FACEBOOK_CLIENT_SECRET=your_facebook_client_secret

# Email настройки (для подтверждения email)
MAILER_DSN=smtp://user:pass@smtp.example.com:587

# Приложение
APP_ENV=dev
APP_SECRET=your_app_secret_here
```

### 5. Запуск базы данных

**Вариант 1: Docker (рекомендуется)**

```bash
docker-compose up -d
```

**Вариант 2: Локальная PostgreSQL**

Убедитесь, что PostgreSQL запущен и создана база данных.

### 6. Создание структуры базы данных

```bash
php bin/console doctrine:migrations:migrate
php bin/console doctrine:fixtures:load
```

### 7. Сборка assets

```bash
npm run build
```

### 8. Запуск сервера разработки

```bash
php bin/console cache:clear
symfony server:start
```

Приложение будет доступно по адресу: `https://127.0.0.1:8000`

## 🔧 Доступные команды

### Symfony команды

```bash
# Очистка кэша
php bin/console cache:clear

# Создание миграций (после изменений в entities)
php bin/console doctrine:migrations:diff
php bin/console doctrine:migrations:migrate

# Загрузка фикстур
php bin/console doctrine:fixtures:load

# Создание пользователя-администратора
php bin/console app:create-admin
```

### Asset команды

```bash
# Сборка для разработки
npm run dev

# Сборка для production
npm run build

# Автоматическая пересборка при изменениях
npm run watch

# Запуск dev сервера с hot reload
npm run dev-server
```

## 🌐 Использование

### Регистрация и вход

1. Перейдите на главную страницу
2. Нажмите "Войти" в правом верхнем углу
3. Выберите вход через Google или Facebook
4. Или зарегистрируйтесь через форму (требует подтверждения email)

### Создание инвентаря

1. После входа нажмите "Создать инвентарь"
2. Заполните название, описание, выберите категорию
3. Настройте кастомные поля на вкладке "Custom Fields"
4. Настройте формат ID на вкладке "Custom Inventory Numbers"
5. Сохраните инвентарь

### Добавление элементов

1. Перейдите в созданный инвентарь
2. Нажмите "Добавить элемент"
3. Заполните форму согласно настроенным полям
4. Сохраните элемент

### Управление доступом

1. В инвентаре перейдите на вкладку "Access Management"
2. Добавьте пользователей по email или username
3. Установите уровень доступа (чтение/запись)

### Поиск и фильтрация

- Используйте поисковую строку в header для полнотекстового поиска
- Фильтруйте инвентари по категориям и тегам
- В инвентаре фильтруйте элементы по любым полям

## 📁 Структура проекта

```
src/
├── Controller/          # Контроллеры
│   ├── AdminController.php
│   ├── HomeController.php
│   ├── InventoryController.php
│   ├── ItemController.php
│   └── ...
├── Entity/              # Сущности Doctrine
│   ├── User.php
│   ├── Inventory.php
│   ├── Item.php
│   └── ...
├── Repository/          # Репозитории
├── Service/             # Сервисы бизнес-логики
├── Security/            # Компоненты безопасности
└── Form/                # Формы

templates/               # Twig шаблоны
assets/                  # Frontend ресурсы
migrations/              # Миграции базы данных
config/                  # Конфигурация Symfony
```

## 🔒 Безопасность

- **OAuth2 аутентификация** через Google и Facebook
- **Ролевая модель**: USER, ADMIN
- **CSRF защита** на всех формах
- **Email верификация** при регистрации
- **Оптимистическая блокировка** для предотвращения конфликтов
- **Валидация** всех входных данных

## 🌍 Многоязычность

Приложение поддерживает два языка:
- Русский (по умолчанию)
- English

Переключение языка доступно в header навигации.

## 🌓 Темы оформления

- **Светлая тема** (по умолчанию)
- **Темная тема**

Переключение темы доступно в header навигации.

## 📊 API Endpoints

Приложение предоставляет REST API для некоторых функций:

- `GET /api/tags` - Автодополнение тегов
- `GET /api/inventories/{id}/items` - Получение элементов инвентаря
- `POST /api/inventories/{id}/comments` - Добавление комментария
- `POST /api/items/{id}/like` - Добавление/удаление лайка

## 🚀 Деплой в production

1. Настройте production переменные окружения
2. Соберите assets: `npm run build`
3. Очистите кэш: `php bin/console cache:clear --env=prod`
4. Запустите миграции: `php bin/console doctrine:migrations:migrate --env=prod`
5. Настройте веб-сервер (Apache/Nginx) для обслуживания `public/index.php`

## 🧪 Тестирование

```bash
# Запуск всех тестов
php bin/phpunit

# Запуск конкретного теста
php bin/phpunit tests/Controller/HomeControllerTest.php
```

## 📚 Дополнительная документация

- [Структура базы данных](DATABASE.md)
- [Руководство пользователя](USER_GUIDE.md)
- [API документация](API.md)

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте feature ветку: `git checkout -b feature/amazing-feature`
3. Зафиксируйте изменения: `git commit -m 'Add amazing feature'`
4. Отправьте в ветку: `git push origin feature/amazing-feature`
5. Создайте Pull Request

## 📄 Лицензия

Этот проект является проприетарным и предназначен для учебных целей.

## 📞 Поддержка

При возникновении проблем:
1. Проверьте [PROGRESS.md](PROGRESS.md) на наличие известных проблем
2. Создайте issue в репозитории
3. Опишите проблему максимально детально
